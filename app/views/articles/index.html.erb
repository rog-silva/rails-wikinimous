<div class="container">
  <% @articles.each do |article| %>
      <article>
        <h5><%= link_to article.title, article %></h2>
        <%= article.content[0..100] %>
      </article>
    <% end %>
</div>
